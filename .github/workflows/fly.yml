name: Fly Deploy
on:
 push:
   branches:
         - main 
env:
 FLY_API_TOKEN: fo1_Ml96keC1Xw4pHwQHPlfdJeX4UIlN2IMra42LtlCPUpc
jobs:
 deploy:
     name: Deploy app
     runs-on: ubuntu-latest
     steps:
       - uses: actions/checkout@v3

       - uses: superfly/flyctl-actions/setup-flyctl@master
      
       - run: flyctl deploy --remote-only