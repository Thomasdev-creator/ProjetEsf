{% extends 'base.html.twig' %}

{% block title %}Cars index{% endblock %}

{% block body %}
    <h1>Cars index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Content</th>
                <th>Price</th>
                <th>Kilometer</th>
                <th>Year</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for car in cars %}
            <tr>
                <td>{{ car.id }}</td>
                <td>{{ car.title }}</td>
                <td>{{ car.content }}</td>
                <td>{{ car.price }}</td>
                <td>{{ car.kilometer }}</td>
                <td>{{ car.year }}</td>
                <td>
                    <a href="{{ path('app_admin_cars_show', {'id': car.id}) }}">show</a>
                    <a href="{{ path('app_admin_cars_edit', {'id': car.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_admin_cars_new') }}">Create new</a>
{% endblock %}
